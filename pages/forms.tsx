import Head from 'next/head';
import { Fragment } from 'react';

const Forms = () => {
    return (
        <Fragment>
            <Head>
                <title>Forms</title>
            </Head>

            <div>Forms</div>
        </Fragment>
    );
};

export default Forms;
